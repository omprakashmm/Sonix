{
  "name": "sonix",
  "version": "1.0.0",
  "private": true,
  "description": "Sonix — ad-free music streaming. Monorepo root scripts.",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:all": "npx concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "typecheck": "npm run typecheck --prefix frontend && npm run typecheck --prefix backend"
  }
}
